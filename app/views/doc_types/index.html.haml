%h1= t(".title")

.panel.panel-primary
  .panel-heading
    = t("doc_types")
  %table.table.table-hover
    %tr
      %th= t("doc_type.name")
      %th= t("action")
    - @doc_types.each do |doc_type|
      %tr
        %td= doc_type.name
        %td.col-md-2
          .btn-group
            = link_to 'Show', doc_type, class: "btn btn-primary"
            %button.btn.btn-primary.dropdown-toggle{"data-toggle" => "dropdown", :type => "button"}
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li= link_to t("edit"), edit_doc_type_path(doc_type)
              %li.divider
              %li= link_to t("destroy"), doc_type, method: :delete, data: { confirm: t("confirm") }
= link_to t("doc_type.new_doc_type"), new_doc_type_path, class: "btn btn-success"
